apiVersion: v1
kind: Service
metadata:
  name: api-loadbalancer
  namespace: api-rest-jwt-spring
spec:
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer